import React from 'react';
import {Icon} from '@ui-kitten/components';

export const HomeIcon = props => <Icon {...props} name="home" pack="assets" />;

export const GlobeIcon = props => (
  <Icon {...props} name="globe" pack="assets" />
);
export const NotifyIcon = props => (
  <Icon {...props} name="notify" pack="assets" />
);
export const ProfileIcon = props => (
  <Icon {...props} name="profile" pack="assets" />
);

export const LogoIcon = props => <Icon {...props} name="logo" pack="assets" />;
export const PlayIcon = props => <Icon {...props} name="play" pack="assets" />;

export const MicIcon = props => <Icon {...props} name="mic" pack="assets" />;
